iNextPD 0.3.0
----------------------------------------------------------------
- Fixes to pass `R CMD check --as-cran` in R-devel.
- Fixes bootstraping estimation for `"incidence_raw"` in `iNext.Sam()`.

iNextPD 0.3.1
----------------------------------------------------------------
- Import `girdExtra`, `ggthemes` to pass R check
- Do not run a example in `estimatePD`
